global:
  scrape_interval: 5s          # Collect metrics every 5 seconds
  evaluation_interval: 5s

# Connect Alertmanager
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "alertmanager:9093"

# Load alert rules files
rule_files:
  - "alert-rules.yml"

# Targets to scrape metrics from
scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "swarm-master"
    static_configs:
      - targets: ["192.168.56.10:9100"]
        labels:
          instance_name: "swarm-master"

  - job_name: "swarm-worker1"
    static_configs:
      - targets: ["192.168.56.11:9100"]
        labels:
          instance_name: "swarm-worker1"

  - job_name: "swarm-worker2"
    static_configs:
      - targets: ["192.168.56.12:9100"]
        labels:
          instance_name: "swarm-worker2"
